{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run annotron extension",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/extension.js"]
    }
  ]
}
