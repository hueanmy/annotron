---
description: Open the current HTML artifact in the annotron review editor and start listening for feedback.
---

Open the most recent HTML artifact in the annotron editor and begin the feedback loop:
run `annotron <artifact>` then `annotron poll <artifact>`, and apply feedback as it arrives.
